{% for field in fields %}
{% if field.deprecated is not null %}
	/**
	 * @deprecated {{ field.deprecated }}
	 */
{% endif %}
	{% if scalarTypeHints %}public {% endif %}const FIELD_{{ field.name | underscore | upper }} = '{{ field.name }}';
{% endfor %}
